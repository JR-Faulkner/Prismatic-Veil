name: Temporary Auryi actor ranged FX patch
on:
  workflow_dispatch:
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Patch Duo-Hybrid driver
        run: python .github/scripts/patch_auryi_actor_ranged_fx.py
      - name: Commit patch
        run: |
          git config user.name "JR-Faulkner"
          git config user.email "53619182+JR-Faulkner@users.noreply.github.com"
          git add src/prizim/DuoHybridSequenceDriver.js
          git commit -m "Render Auryi as clean ranged actor plus FX" || exit 0
          git push
