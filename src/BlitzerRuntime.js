// Compatibility shim for older cached Blitzer configs.
// The runtime asset is now a normal repository WebP, so no async boot work is required.
export const BLITZER_SHEET_DATA_URI = './assets/characters/kineza/animations/kineza_blitzer_basic_v1.webp';
